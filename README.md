# Baseline-Model-Auto-Generation-
AI framework for interpreting building-energy-code logic and generating Appendix-G–style baseline model specs. Uses synthetic + open data to extract rule structures into JSON, applies a Python rule engine for baseline generation, and uses LLMs for parsing and explanations.
